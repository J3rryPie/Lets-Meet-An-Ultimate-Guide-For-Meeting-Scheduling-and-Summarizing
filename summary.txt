for your ears for years now the master of intrigueLatent Semantic Analysis is an unsupervised approach that uses the context of the input document and extracts information such as which words are used together and which common words are seen in different sentences.Advantage of LSA vectors over word vectors is that semantic relations as represented in the human brain are captured automatically by LSA vectors while word vectors require explicit methods to originate those semantic relations.